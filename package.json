{
  "name": "cypress-test",
  "version": "1.0.0",
  "description": "cypress based test",
  "main": "index.js",
  "scripts": {
    "format": "npx prettier --write .",
    "lint": "eslint **/*.js",
    "clean": "rm -rf results",
    "cy:open": "cypress open",
    "cy:run": "npm run clean && cypress run --browser chrome --headed"
  },
  "author": "Ramesh Rengasamy",
  "devDependencies": {
    "cypress": "^13.3.0",
    "cypress-mochawesome-reporter": "^3.6.0",
    "cypress-real-events": "^1.10.3",
    "eslint": "^8.50.0",
    "eslint-plugin-cypress": "^2.15.1",
    "prettier": "3.0.3"
  }
}
